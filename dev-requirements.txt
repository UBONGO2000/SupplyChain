# Development Dependencies
# ========================
# Install with: pip install -r requirements.txt -r dev-requirements.txt

# Testing
pytest==7.4.0
pytest-asyncio==0.23.3
pytest-cov==7.1.0
# NOTE: pinned <0.28 -- httpx 0.28 dropped the `app=` shortcut that
# starlette's TestClient (bundled with the fastapi==0.109.0 pin above) relies
# on, which breaks fastapi.testclient.TestClient outright.
httpx==0.27.2

# Code Quality
black==24.1.1
isort==5.13.2
flake8==7.0.0
mypy==1.8.0

# Security (used by .github/workflows/security.yml)
bandit==1.9.4
pip-audit==2.10.1
